
## CE-MRI-synthesis 多序列融合方式分析

### 1. 输入序列配置

项目在 [`ce_mri_param.py`](file:///mnt/data/KASR/Dengsiyi/MRI_GAN/CE-MRI-synthesis/training_project/ce_mri_param.py#L13) 中定义了默认的输入序列：

```python
parser.add_argument('--train_keys', type=list, default=["t1", "t2", "b50", "b800", "b1500", "adc"])
```

支持的 MRI 序列包括：
- **T1**: T1 加权图像
- **T2**: T2 加权图像  
- **b50**: DWI b=50 s/mm²
- **b800**: DWI b=800 s/mm²
- **b1500**: DWI b=1500 s/mm²
- **ADC**: 表观扩散系数图

### 2. 多序列融合方式 - **通道拼接（Channel Concatenation）**

该项目采用 **早期融合（Early Fusion）** 策略，即在输入层将多序列图像在通道维度上进行拼接：

#### 2.1 生成器输入通道计算

在 [`trainer_pix2pix_mulD.py`](file:///mnt/data/KASR/Dengsiyi/MRI_GAN/CE-MRI-synthesis/training_project/trainer_pix2pix_mulD.py#L64) 中：

```python
input_channels = len(config.train_keys)  # 输入通道数 = 序列数量（默认6）
output_channel = 1  # 输出通道数 = 1（生成CE-MRI）

self.net_G = Unet(
    encoder_name='timm-regnety_160',
    encoder_weights='imagenet',
    encoder_depth=4,
    decoder_channels=[128, 64, 32, 16],
    decoder_use_batchnorm=True,
    in_channels=input_channels,  # 6个通道输入
    classes=1  # 1个通道输出
)
```

#### 2.2 判别器输入通道计算

在 [`trainer_pix2pix_mulD.py`](file:///mnt/data/KASR/Dengsiyi/MRI_GAN/CE-MRI-synthesis/training_project/trainer_pix2pix_mulD.py#L74) 中：

```python
self.net_D = define_D(
    input_nc=input_channels + output_channel,  # 6 + 1 = 7个通道
    ndf=64,
    n_layers_D=config.n_layers_D,
    norm="instance",
    use_sigmoid=False,
    num_D=config.num_D,
    getIntermFeat=True if "G_Feat_loss" in config.loss_weight_dict.keys() else False
)
```

### 3. 数据预处理流程

#### 3.1 数据存储格式

在 [`preprocess_h5_2d.py`](file:///mnt/data/KASR/Dengsiyi/MRI_GAN/CE-MRI-synthesis/preprocessing_nii/preprocess_h5_2d.py) 中，每个序列被独立存储为 H5 文件：

```python
h5_file["t1"] = t1_array[layer, :, :]
h5_file["t2"] = t2_array[layer, :, :]
h5_file["b800"] = b800_array[layer, :, :]
h5_file["b50"] = b50_array[layer, :, :]
h5_file["t1ce"] = t1ce_array[layer, :, :]  # 目标输出
h5_file["b1500"] = b1500_array[layer, :, :]
h5_file["adc"] = adc_array[layer, :, :]
```

#### 3.2 数据加载

在 [`my_transform.py`](file:///mnt/data/KASR/Dengsiyi/MRI_GAN/CE-MRI-synthesis/training_project/utils/my_transform.py#L10) 中定义了 `LoadH5` 类：

```python
class LoadH5(MapTransform):
    def __init__(self, path_key, keys: KeysCollection):
        super().__init__(keys)
        self.path_key = path_key

    def __call__(self,data):
        d = dict(data)
        h5_file = h5py.File(d[self.path_key])
        for key in self.keys:
            d[key] = h5_file[key][()]  # 按key加载各序列
        return d
```

### 4. 训练时的数据流

在 [`trainer_pix2pix_mulD.py`](file:///mnt/data/KASR/Dengsiyi/MRI_GAN/CE-MRI-synthesis/training_project/trainer_pix2pix_mulD.py#L330-L333) 的训练步骤中：

```python
def training_step(self, batch, batch_idx):
    real_A, real_B = (batch["image"], batch["t1ce"])  # real_A是多序列拼接的输入，real_B是目标CE-MRI
    optimizer_G, optimizer_D = self.optimizers()
    fake_B = self.forward(real_A)  # 前向传播生成CE-MRI
```

**注意**：`batch["image"]` 是通过 MONAI 的 `ConcatItemsd` 变换将多个序列在通道维度拼接后的结果。

### 5. 融合方式总结

| 特性 | 说明 |
|------|------|
| **融合策略** | 早期融合（Early Fusion） |
| **融合位置** | 网络输入层 |
| **融合方式** | 通道拼接（Channel Concatenation） |
| **输入维度** | `[B, C, H, W]`，其中 C = 序列数量（默认6） |
| **输出维度** | `[B, 1, H, W]`，生成单通道 CE-MRI |
| **骨干网络** | RegNet (timm-regnety_160) + UNet 解码器 |

### 6. 架构图示

```
多序列输入 (T1, T2, b50, b800, b1500, ADC)
        ↓
    [通道拼接]
        ↓
   [B, 6, H, W] ──→ UNet 编码器 (RegNet) ──→ 解码器 ──→ [B, 1, H, W]
        ↓                                              ↓
   输入判别器 D                                    生成 CE-MRI
```

这种融合方式的优点是实现简单、计算效率高，让网络自动学习多序列间的特征关联；缺点是无法显式建模序列间的复杂关系。